<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Birthday 💫</title>
<style>
/* ---------- GLOBAL STYLES ---------- */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap');
:root {
  --bg-color: #0d0d10;
  --text-color: #fff;
  --accent: #ff6ec7;
  --secondary: #03dac6;
  --blur: rgba(255,255,255,0.08);
}
* {
  margin: 0; padding: 0;
  box-sizing: border-box;
  font-family: 'Poppins', sans-serif;
  color: var(--text-color);
}
body {
  background: radial-gradient(circle at top left, #1b1b25, #0d0d10);
  overflow: hidden;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  position: relative;
}
.hidden { display: none; }

/* ---------- BACKGROUND ANIMATIONS ---------- */
canvas {
  position: absolute;
  top: 0; left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}
.glow {
  position: absolute;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle, rgba(255,110,199,0.2), transparent 70%);
  filter: blur(150px);
  z-index: 0;
  animation: pulse 6s ease-in-out infinite alternate;
}
@keyframes pulse {
  from { transform: scale(1); opacity: 0.8; }
  to { transform: scale(1.4); opacity: 0.3; }
}

/* ---------- CARD STYLES ---------- */
.card {
  position: relative;
  z-index: 2;
  background: var(--blur);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 25px;
  padding: 40px;
  text-align: center;
  max-width: 420px;
  transition: all 0.8s ease;
  box-shadow: 0 0 25px rgba(255,110,199,0.1);
}
h1 {
  font-size: 2em;
  margin-bottom: 10px;
}
p {
  font-size: 1em;
  opacity: 0.8;
}
button {
  margin-top: 25px;
  padding: 12px 28px;
  border: none;
  background: linear-gradient(90deg, var(--accent), var(--secondary));
  color: #fff;
  border-radius: 25px;
  font-weight: 600;
  font-size: 1em;
  cursor: pointer;
  transition: 0.3s;
}
button:hover {
  transform: scale(1.05);
  box-shadow: 0 0 20px var(--accent);
}

/* ---------- MESSAGE ANIMATION ---------- */
.fade {
  animation: fadeIn 1s ease forwards;
}
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(15px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ---------- CONFETTI ---------- */
.confetti {
  position: absolute;
  width: 10px;
  height: 10px;
  background: var(--accent);
  top: 0;
  opacity: 0.8;
  animation: fall 3s linear infinite;
}
@keyframes fall {
  to { transform: translateY(110vh) rotate(360deg); opacity: 0; }
}

/* ---------- HEART ANIMATION ---------- */
@keyframes floatHeart {
  0% { transform: translateY(0) scale(0.8); opacity: 1; }
  100% { transform: translateY(-200px) scale(1.4); opacity: 0; }
}
.heart {
  position: absolute;
  color: #ff6ec7;
  font-size: 20px;
  animation: floatHeart 4s linear infinite;
}
</style>
</head>
<body>

<canvas id="stars"></canvas>
<div class="glow"></div>

<!-- Greeting Cards -->
<div class="card fade" id="step1">
  <h1>🎉 Hey, You! 🎉</h1>
  <p>There’s something special waiting for you...</p>
  <button onclick="nextStep(1)">Let’s Begin 💫</button>
</div>

<div class="card hidden" id="step2">
  <h1>💖 Happy Birthday! 💖</h1>
  <p>Today’s your day — the universe shines a little brighter just for you.</p>
  <button onclick="nextStep(2)">Next 🌙</button>
</div>

<div class="card hidden" id="step3">
  <h1>🌸 You Are Amazing 🌸</h1>
  <p>Your smile, your kindness, your energy — everything about you lights up the world.</p>
  <button onclick="nextStep(3)">Continue ✨</button>
</div>

<div class="card hidden" id="step4">
  <h1>💌 A Little Secret 💌</h1>
  <p>Someone out there (maybe me 👀) feels incredibly lucky to know you.</p>
  <button onclick="nextStep(4)">Final Surprise 🎁</button>
</div>

<div class="card hidden" id="final">
  <h1>🎂 Happy Birthday, Beautiful Soul 🎂</h1>
  <p>May your year ahead be filled with love, laughter, and unforgettable moments.</p>
  <p style="margin-top:10px; font-weight:500;">You deserve every bit of happiness in this universe. 💖</p>
  <button onclick="celebrate()">Celebrate 🎊</button>
</div>

<audio id="bgmusic" loop>
  <source src="https://cdn.pixabay.com/download/audio/2023/03/13/audio_6e37a7e52e.mp3?filename=romantic-piano-145253.mp3" type="audio/mp3">
</audio>

<script>
/* ---------- INTERACTIVE FLOW ---------- */
function nextStep(num) {
  document.getElementById("step" + num).classList.add("hidden");
  const next = num === 4 ? "final" : "step" + (num + 1);
  const card = document.getElementById(next);
  card.classList.remove("hidden");
  card.classList.add("fade");

  // Play music on first interaction
  if (num === 1) document.getElementById("bgmusic").play();
}

/* ---------- CELEBRATION ---------- */
function celebrate() {
  for (let i = 0; i < 80; i++) createConfetti();
  for (let i = 0; i < 15; i++) createHeart();
}

function createConfetti() {
  const confetti = document.createElement("div");
  confetti.classList.add("confetti");
  confetti.style.left = Math.random() * 100 + "vw";
  confetti.style.background = `hsl(${Math.random()*360}, 100%, 60%)`;
  confetti.style.animationDuration = 2 + Math.random() * 3 + "s";
  document.body.appendChild(confetti);
  setTimeout(() => confetti.remove(), 5000);
}

function createHeart() {
  const heart = document.createElement("div");
  heart.classList.add("heart");
  heart.style.left = Math.random() * 100 + "vw";
  heart.style.fontSize = (15 + Math.random() * 25) + "px";
  heart.innerHTML = "❤";
  document.body.appendChild(heart);
  setTimeout(() => heart.remove(), 4000);
}

/* ---------- BACKGROUND STARS ---------- */
const canvas = document.getElementById("stars");
const ctx = canvas.getContext("2d");
let stars = [];
resize();
window.addEventListener("resize", resize);

function resize() {
  canvas.width = innerWidth;
  canvas.height = innerHeight;
  stars = Array(100).fill().map(() => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height,
    size: Math.random() * 1.5,
    speed: 0.1 + Math.random() * 0.3
  }));
}

function animate() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  ctx.fillStyle = "#fff";
  for (let s of stars) {
    ctx.beginPath();
    ctx.arc(s.x, s.y, s.size, 0, Math.PI * 2);
    ctx.fill();
    s.y += s.speed;
    if (s.y > canvas.height) s.y = 0;
  }
  requestAnimationFrame(animate);
}
animate();
</script>

</body>
</html>